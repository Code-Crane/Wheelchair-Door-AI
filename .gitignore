# ───── Python 기본 캐시 및 빌드 파일 ─────
__pycache__/
*.py[cod]
*.so
*.log
*.tmp
*.bak
# ───── 가상환경 ─────
env/
.venv/
.ENV/
venv/
*.env
# ───── 모델 및 대용량 파일 ─────
*.h5
*.hdf5
*.keras
# ───── 데이터셋 (용량 초과 방지, .gitkeep로 구조 유지) ─────
dataset/
!dataset/.gitkeep
# ───── 이미지 결과물 (시각화용 제외) ─────
images/generated/
outputs/
# ───── 기타 캐시 ─────
.cache/
.pytest_cache/
.mypy_cache/
.ruff_cache/
